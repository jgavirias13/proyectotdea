{{>header}}

<div class="container">
    <h2>Contactanos</h2>

    {{{mensajeExito}}}
    {{{mensajeError}}}

    <form action="/contactanos" method="POST">

        <div class="form-group row">
            <label for="asunto" class="col-sm-2 col-form-label">Asunto</label>
            <div class="col-sm-10">
                <input type="text" class="form-control" id="asunto" name="asunto"
                    placeholder="Asunto del correo" required="true">
            </div>
        </div>

        <div class="form-group row">
            <label for="contenido" class="col-sm-2 col-form-label">Contenido</label>
            <div class="col-sm-10">
                <textarea class="form-control" id="contenido" name="contenido" rows="3"
                    placeholder="Cuentanos tus inquietudes..." required="true"></textarea>
            </div>
        </div>

        <div class="form-group row ">
            <div class="col-sm-6">
                <button class="btn btn-primary">Enviar</button>
            </div>
        </div>
    </form>
</div>

{{>footer}}